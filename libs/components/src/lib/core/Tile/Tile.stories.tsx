import type { Meta } from '@storybook/react';
import Tile from './Tile';

const Story: Meta<typeof Tile> = {
  component: Tile,
  title: 'Tile',
};
export default Story;

export const Default = {
  args: {},
};
